# Example YAML Flows

Đây là ví dụ tham khảo từ chuỗi ICT — KHÔNG phải test cases active.

Dùng để hiểu format YAML flow, cách dùng `use:` (shared flows), assertions, variables.

Xem thêm `src/types.ts` cho full schema.
