# frt-test credentials & overrides — KHÔNG commit file này (.gitignore đã ignore .env)
# Điền giá trị rồi đổi tên .env.example → .env (hoặc tạo .env mới).
#
# Credentials (precedence: TEST_<APP>_<ENV>_<KEY> > TEST_<APP>_<KEY> > TEST_<KEY>):
TEST_USERNAME=
TEST_PASSWORD=
# Ví dụ scoped theo app/env:
# TEST_RSA_LAB_CI_USERNAME=
# TEST_RSA_LAB_CI_PASSWORD=
